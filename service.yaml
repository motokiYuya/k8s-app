apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80  # クラスタ内で使用するポート
      targetPort: 80  # アプリケーションがリッスンしているポート


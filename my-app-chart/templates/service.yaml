apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}  # クラスタ内で使用するポート
      targetPort: {{ .Values.service.port }}  # アプリケーションがリッスンしているポート